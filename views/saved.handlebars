<div class="container">
  <div class="jumbotron">
    <h1>NBA Scraper</h1>
    <hr>
  </div>
  <div class="row">


    <div class="col-lg-12">

      <div class="card">
        <div class="card-header">
       Here are your saved articles!
        </div>
        <div class="card-body" id="well-section">
          {{#each renderData}}
          <div class="headline">
            <p>{{title}}</p>
            <a href={{link}} target="_blank">Jump to the full story.</a>
            <button class="save">Save Article</button>
          </div>
          <br>
          {{/each}}
        </div>
      </div>
    </div>
  </div>
</div>